//제곱 ㄴㄴ수
#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main(void){
    ll a b;
    cin >> a >> b;
    ll gap=b-a+1;
    ll ans=gap;
    vector<bool> v(gap, false);
    ll i=2, seq=1;
    while(i*i*n<=b){
        for(n=1; seq<)
        
        i++;

        if(i*i>b){

        }
    }

    return 0;
}