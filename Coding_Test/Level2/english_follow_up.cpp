#include <string>
#include <vector>
#include <iostream>
#include <iterator>

using namespace std;

// template <class T>
// ostream& operator<<(ostream& out, const vector<T>& values){
//     copy(begin(value), end(value), ostream_iterator<T>(stream, ","));
// }

// vector<int> solution(int n, vector<string> words) {
//     vector<int> answer;
//     answer.push_back(1);
//     answer.push_back(1);
//     answer.push_back(1);

//     return answer;
// }

int main(void){

    vector<string> words = {"tank", "kick", "know", "wheel", "land", "dream", "mother", "robot", "tank"};
    int n = 3;

    //vector<int> answer = solution(n, words);

    //cout << answer << endl;
    cout <<"hi"; 
    //<<endl;

    return 0;
}